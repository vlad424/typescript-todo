import { ITask } from "../../types/redux_state";
import { IPutArrayPosts, IPutPost } from "../../types/rtk.types";

export const multiService = ({
  sendNanQuery: (data: IPutPost | IPutArrayPosts) => {
    
  }
})